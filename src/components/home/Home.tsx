'use client';
import React from 'react';
import Body from '../body/Body';

const HomePage = () => {
    return (
        <React.Fragment>
            <Body />
        </React.Fragment>
    );
};

export default HomePage;